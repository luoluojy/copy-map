import { SidenavWatchDirective } from './sidenav-watch.directive';

describe('SidenavWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavWatchDirective();
    expect(directive).toBeTruthy();
  });
});
