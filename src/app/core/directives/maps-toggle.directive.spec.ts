import { MapsToggleDirective } from './maps-toggle.directive';

describe('MapsToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new MapsToggleDirective();
    expect(directive).toBeTruthy();
  });
});
