import { SearchContainerWatchDirective } from './search-container-watch.directive';

describe('SearchContainerWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchContainerWatchDirective();
    expect(directive).toBeTruthy();
  });
});
