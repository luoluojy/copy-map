import { BottomContainerWatchDirective } from './bottom-container-watch.directive';

describe('BottomContainerWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomContainerWatchDirective();
    expect(directive).toBeTruthy();
  });
});
