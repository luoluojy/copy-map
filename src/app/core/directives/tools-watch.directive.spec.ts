import { ToolsWatchDirective } from './tools-watch.directive';

describe('ToolsWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolsWatchDirective();
    expect(directive).toBeTruthy();
  });
});
