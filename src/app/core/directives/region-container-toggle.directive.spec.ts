import { RegionContainerToggleDirective } from './region-container-toggle.directive';

describe('RegionContainerToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new RegionContainerToggleDirective();
    expect(directive).toBeTruthy();
  });
});
