import { CommonClickDirective } from './common-click.directive';

describe('CommonClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonClickDirective();
    expect(directive).toBeTruthy();
  });
});
