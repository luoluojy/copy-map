import { MapsWatchDirective } from './maps-watch.directive';

describe('MapsWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new MapsWatchDirective();
    expect(directive).toBeTruthy();
  });
});
