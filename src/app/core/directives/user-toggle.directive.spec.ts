import { UserToggleDirective } from './user-toggle.directive';

describe('UserToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new UserToggleDirective();
    expect(directive).toBeTruthy();
  });
});
