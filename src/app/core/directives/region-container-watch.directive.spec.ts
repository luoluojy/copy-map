import { RegionContainerWatchDirective } from './region-container-watch.directive';

describe('RegionContainerWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new RegionContainerWatchDirective();
    expect(directive).toBeTruthy();
  });
});
