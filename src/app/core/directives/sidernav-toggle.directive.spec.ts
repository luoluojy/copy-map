import { SidernavToggleDirective } from './sidernav-toggle.directive';

describe('SidernavToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SidernavToggleDirective();
    expect(directive).toBeTruthy();
  });
});
