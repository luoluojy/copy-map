import { SearchContainerToggleDirective } from './search-container-toggle.directive';

describe('SearchContainerToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchContainerToggleDirective();
    expect(directive).toBeTruthy();
  });
});
