import { BottomContainerToggleDirective } from './bottom-container-toggle.directive';

describe('BottomContainerToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomContainerToggleDirective();
    expect(directive).toBeTruthy();
  });
});
