import { ToolsToggleDirective } from './tools-toggle.directive';

describe('ToolsToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolsToggleDirective();
    expect(directive).toBeTruthy();
  });
});
