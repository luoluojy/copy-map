import { NoticeContainerWatchDirective } from './notice-container-watch.directive';

describe('NoticeContainerWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new NoticeContainerWatchDirective();
    expect(directive).toBeTruthy();
  });
});
