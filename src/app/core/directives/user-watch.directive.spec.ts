import { UserWatchDirective } from './user-watch.directive';

describe('UserWatchDirective', () => {
  it('should create an instance', () => {
    const directive = new UserWatchDirective();
    expect(directive).toBeTruthy();
  });
});
