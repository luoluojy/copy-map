import { ClickedStyleDirective } from './clicked-style.directive';

describe('ClickedStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedStyleDirective();
    expect(directive).toBeTruthy();
  });
});
