import { ControlViewDirective } from './control-view.directive';

describe('ControlViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlViewDirective();
    expect(directive).toBeTruthy();
  });
});
